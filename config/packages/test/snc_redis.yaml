snc_redis:
    clients:
        default:
            type: phpredis
            alias: default
            dsn: "%env(ASTRADA_REDIS_DSN)%/1"
            options:
                prefix: "%database_name%_test:"