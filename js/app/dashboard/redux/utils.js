import _ from 'lodash'
import moment from '../../moment'

export const isListingVisible = (listing, filters, date) => {

    // const {
    //     tags,
    //     timeRange,
    // } = filters


    return true
}