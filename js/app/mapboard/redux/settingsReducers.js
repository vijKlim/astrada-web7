
const initialState = {
    clustersEnabled: false,
    polylineStyle: 'normal',
    listingsPagination: {
        pages_count: 0,
        page: 0,

    }
}

export default (state = initialState, action) => {

    return state
}