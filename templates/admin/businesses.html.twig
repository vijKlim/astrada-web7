{% extends "admin.html.twig" %}

{% block breadcrumb %}
    <li>{% trans %}adminDashboard.businesses.title{% endtrans %}</li>
{% endblock %}

{% block content %}

    <div class="d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center flex-fill">
            <div id="search-businesses" class="w-25 mr-4">
                <input type="text" class="form-control">
            </div>
{#            <a href="{{ path('admin_hubs') }}" class="btn btn-default mr-4">#}
{#                {{ 'adminDashboard.hubs.title'|trans }}#}
{#            </a>#}
            <a href="{{ path('admin_promotions') }}" class="btn btn-default mr-4">
                {% trans %}adminDashboard.promotions.title{% endtrans %}
            </a>

        </div>
        <a href="{{ path('admin_business_new') }}" class="btn btn-success">
            <i class="fa fa-plus"></i> {% trans %}adminDashboard.businesses.createNew{% endtrans %}
        </a>
    </div>
    {% include "admin/pagination.html.twig" %}
    {% include "_partials/business/list.html.twig" %}
    {% include "admin/pagination.html.twig" %}

{% endblock %}

{% block scripts %}
    {{ encore_entry_script_tags('admin-businesses') }}
{% endblock %}
