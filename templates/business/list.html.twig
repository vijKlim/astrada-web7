{% extends "base2.html.twig" %}

{% block body %}
{% include "business/_partials/list_header.html.twig" %}
{% include "business/_partials/list_buttons.html.twig" with { to_mode: 'map' } %}
<div class="container">
  {% if count == 0 %}
    <div class="col-md-6 col-md-offset-3 alert alert-warning text-center">
      {{ 'restaurants.no_results'|trans|raw }}
    </div>
  {% else %}
    <div class="row display-flex restaurant-list">
      {% for business in businesses %}
      <div class="col-sm-6 col-md-4 display-flex">
        {% include "_partials/business/list_thumbnail.html.twig" %}
      </div>
      {% endfor %}
    </div>
    <div class="row">
      <div class="col-xs-12">
        {% include "business/pagination.html.twig" %}
      </div>
    </div>
  {% endif %}
</div>
{% endblock %}

{% block styles %}
  {{ encore_entry_link_tags('business-list') }}
{% endblock %}

{% block scripts %}
  {{ encore_entry_script_tags('search-address') }}
  {{ encore_entry_script_tags('business-list') }}
{% endblock %}
